<p align="center">
  <strong>Спутник</strong>
</p>
<p align="center">
  <strong>Спутник</strong> — программа, в которой говоришь и получаешь чистый текст
</p>

> **В этом репозитории — только наш слой Спутника:** сайт (`sputnik-site/`), защита обновлений (`sputnik-guard/`), установщик (`install`) и правки ядра (`patches/opencode-core.diff`). Само ядро — OpenCode, его код лежит в их репозитории github.com/anomalyco/opencode и сюда не копируется.

---

## Что это

Форк [OpenCode](https://github.com/anomalyco/opencode) с добавленным голосовым
слоем. Основу — ядро, шлюз к моделям, терминальный интерфейс, сервер — мы не
трогали. Наше — голос: распознавание речи и приведение её к нормальному
письменному тексту.

## Происхождение

Этот проект создан на основе **OpenCode**, лицензия MIT.

```
Авторские права: Copyright (c) 2025 opencode
OpenCode:  https://github.com/anomalyco/opencode
Лицензия:  MIT — текст в файле LICENSE-OPENCODE, он не изменялся
Оригинальный README: README.opencode.md
```

MIT разрешает копировать, изменять и распространять. Уведомление об авторских
правах сохранено в `LICENSE-OPENCODE` и продублировано здесь. Что наше и что не наше —
подробно в файле `NOTICE`.

**OpenCode** — торговая марка своих владельцев. Этот продукт так не называется:
продукт называется Спутник.

## Установка

```bash
curl -fsSL https://sputnik.ru/install | bash
```

```bash
git clone https://github.com/alexMislitel/sputnik
cd sputnik
bun install
```

Из своего ключа API и адреса сервера, если хочешь гейт к моделям.

## Модели

Поддерживаются 75+ провайдеров — OpenAI, Anthropic, Google, Mistral, Qwen,
любой OpenAI-совместимый адрес, и локальные модели через Ollama.

Локально:

```bash
ollama pull qwen3:8b
ollama serve
```

Приоритет отдаётся локальной модели. Если её нет, обращаемся к внешнему API.

## Голосовой слой

Мы пишем голосом, программа отдаёт текстом:

- слова-паразиты вырезаются кодом, а не моделью — одинаково каждый раз
- ошибки распознавания чинятся по списку
- модель ставит знаки препинания, делит на абзацы, разбирает смысл
- **имена, числа, даты, номера документов переносятся дословно**

Текст не хранится. Ни логов, ни резервных копий, ни обучения.

## Состояние

| Часть | Кто |
|---|---|
| Ядро, шлюз, TUI, сервер, сборка | OpenCode, MIT |
| Голосовой слой, чистильщик, промпты | наше |
| Сайт, авторизация, сообщество | наше, в работе |

## Лицензия

Ядро проекта — MIT, файл `LICENSE-OPENCODE`, не изменяется.
Наш голосовой слой распространяется на условиях автора проекта.

## Контакты

Автор: Alexandr
Репозиторий: https://github.com/alexMislitel/sputnik
